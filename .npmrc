legacy-peer-deps=true

# Note: node-linker=hoisted is set in pnpm-workspace.yaml (nodeLinker), which is
# the authoritative location pnpm honors on a clean workspace install. Required
# for Expo/React Native Metro resolution.
